# Nexform

Multilingual agency website built with Astro and Tailwind CSS.

## Local development

```sh
npm install
npm run dev
```

Copy `.env.example` to `.env` and configure the form and contact integrations before launch.

## Production build

```sh
npm run build
```

The static output is generated in `dist/`.
